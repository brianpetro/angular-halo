<div ng-controller="ProjectCtrl">
	<h1>This is {{ project.details.name }}</h1>

	<h2>Entries</h2>
	<ul ng-repeat=" e in project.entries ">
		<li>
			{{ e.id }}
		</li>
		<li>
			{{ e.title }}
		</li>
		<li>
			{{ e.summary }}
		</li>
	</ul>
</div>
